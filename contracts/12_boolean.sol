// SPDX-License-Identifier: GPL-3.0
pragma solidity >= 0.7.0 < 0.9.0;

contract boolean {
    // Default value of bool is false
    bool public defaultBool;
    // We can init the value as either true or false only
    bool public check = true;
}